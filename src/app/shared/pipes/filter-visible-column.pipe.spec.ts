import { FilterVisibleColumnPipe } from './filter-visible-column.pipe';

describe('FilterVisibleColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVisibleColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
